---
name: 'Run ShellCheck'

on:
  push:
  pull_request:
    branches:
      - 'main'

jobs:
  shellcheck:
    name: ShellCheck
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Run ShellCheck
        uses: ludeeus/action-shellcheck@master
